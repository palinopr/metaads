import { 
  BatchProcessor, 
  BatchItem, 
  BatchResult, 
  BatchConfig,
  BatchBuilder,
  MetaBatchExecutor
} from '../../../lib/data-pipeline/batch-processor'

// Mock fetch for testing
global.fetch = jest.fn()

describe('BatchProcessor', () => {
  let processor: BatchProcessor
  let mockExecutor: jest.Mock

  beforeEach(() => {
    jest.clearAllMocks()
    processor = new BatchProcessor()
    mockExecutor = jest.fn()
  })

  describe('Constructor and Configuration', () => {
    it('should initialize with default configuration', () => {
      const defaultProcessor = new BatchProcessor()
      expect(defaultProcessor.getQueueSize()).toBe(0)
    })

    it('should accept custom configuration', () => {
      const customConfig: Partial<BatchConfig> = {
        maxBatchSize: 25,
        maxConcurrent: 3,
        retryAttempts: 5
      }
      
      const customProcessor = new BatchProcessor(customConfig)
      expect(customProcessor.getQueueSize()).toBe(0)
    })
  })

  describe('Queue Management', () => {
    it('should add single item to queue', () => {
      const item: BatchItem = {
        id: 'test-1',
        method: 'GET',
        relativeUrl: 'campaign/123'
      }
      
      processor.add(item)
      expect(processor.getQueueSize()).toBe(1)
    })

    it('should add multiple items to queue', () => {
      const items: BatchItem[] = [
        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' },
        { id: 'test-2', method: 'GET', relativeUrl: 'campaign/456' },
        { id: 'test-3', method: 'GET', relativeUrl: 'campaign/789' }
      ]
      
      processor.addMultiple(items)
      expect(processor.getQueueSize()).toBe(3)
    })

    it('should clear queue', () => {
      const items: BatchItem[] = [
        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' },
        { id: 'test-2', method: 'GET', relativeUrl: 'campaign/456' }
      ]
      
      processor.addMultiple(items)
      expect(processor.getQueueSize()).toBe(2)
      
      processor.clearQueue()
      expect(processor.getQueueSize()).toBe(0)
    })
  })

  describe('Basic Processing', () => {
    it('should process simple batch without dependencies', async () => {
      const items: BatchItem[] = [
        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' },
        { id: 'test-2', method: 'GET', relativeUrl: 'campaign/456' }
      ]
      
      processor.addMultiple(items)
      
      mockExecutor.mockResolvedValue([
        { data: { id: '123', name: 'Campaign 1' } },
        { data: { id: '456', name: 'Campaign 2' } }
      ])
      
      const results = await processor.process(mockExecutor)
      
      expect(results.size).toBe(2)
      expect(results.get('test-1')?.success).toBe(true)
      expect(results.get('test-2')?.success).toBe(true)
      expect(mockExecutor).toHaveBeenCalledWith(items)
    })

    it('should handle executor errors', async () => {
      const items: BatchItem[] = [
        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }
      ]
      
      processor.addMultiple(items)
      mockExecutor.mockRejectedValue(new Error('Network error'))\n      \n      const results = await processor.process(mockExecutor)\n      \n      expect(results.size).toBe(1)\n      expect(results.get('test-1')?.success).toBe(false)\n      expect(results.get('test-1')?.error?.message).toBe('Network error')\n    })\n\n    it('should apply transformations to responses', async () => {\n      const items: BatchItem[] = [\n        { \n          id: 'test-1', \n          method: 'GET', \n          relativeUrl: 'campaign/123',\n          transform: (response) => ({ ...response.data, transformed: true })\n        }\n      ]\n      \n      processor.addMultiple(items)\n      \n      mockExecutor.mockResolvedValue([\n        { data: { id: '123', name: 'Campaign 1' } }\n      ])\n      \n      const results = await processor.process(mockExecutor)\n      \n      expect(results.get('test-1')?.data).toEqual({\n        id: '123',\n        name: 'Campaign 1',\n        transformed: true\n      })\n    })\n  })\n\n  describe('Dependency Management', () => {\n    it('should process items with dependencies in correct order', async () => {\n      const items: BatchItem[] = [\n        { \n          id: 'dependent', \n          method: 'GET', \n          relativeUrl: 'insights/123',\n          dependencies: ['base']\n        },\n        { \n          id: 'base', \n          method: 'GET', \n          relativeUrl: 'campaign/123'\n        }\n      ]\n      \n      processor.addMultiple(items)\n      \n      mockExecutor\n        .mockResolvedValueOnce([{ data: { id: '123' } }]) // base first\n        .mockResolvedValueOnce([{ data: { insights: 'data' } }]) // dependent second\n      \n      const results = await processor.process(mockExecutor)\n      \n      expect(results.size).toBe(2)\n      expect(mockExecutor).toHaveBeenCalledTimes(2)\n      \n      // Verify base was called first\n      expect(mockExecutor.mock.calls[0][0]).toEqual([\n        { id: 'base', method: 'GET', relativeUrl: 'campaign/123' }\n      ])\n    })\n\n    it('should detect circular dependencies', async () => {\n      const items: BatchItem[] = [\n        { \n          id: 'item-1', \n          method: 'GET', \n          relativeUrl: 'test/1',\n          dependencies: ['item-2']\n        },\n        { \n          id: 'item-2', \n          method: 'GET', \n          relativeUrl: 'test/2',\n          dependencies: ['item-1']\n        }\n      ]\n      \n      processor.addMultiple(items)\n      \n      await expect(processor.process(mockExecutor))\n        .rejects.toThrow('Circular dependencies detected')\n    })\n\n    it('should handle complex dependency chains', async () => {\n      const items: BatchItem[] = [\n        { id: 'level-0-a', method: 'GET', relativeUrl: 'test/a' },\n        { id: 'level-0-b', method: 'GET', relativeUrl: 'test/b' },\n        { \n          id: 'level-1-a', \n          method: 'GET', \n          relativeUrl: 'test/1a',\n          dependencies: ['level-0-a']\n        },\n        { \n          id: 'level-1-b', \n          method: 'GET', \n          relativeUrl: 'test/1b',\n          dependencies: ['level-0-b']\n        },\n        { \n          id: 'level-2', \n          method: 'GET', \n          relativeUrl: 'test/2',\n          dependencies: ['level-1-a', 'level-1-b']\n        }\n      ]\n      \n      processor.addMultiple(items)\n      \n      mockExecutor.mockResolvedValue([{ data: 'success' }])\n      \n      const results = await processor.process(mockExecutor)\n      \n      expect(results.size).toBe(5)\n      expect(mockExecutor).toHaveBeenCalledTimes(3) // 3 dependency levels\n    })\n  })\n\n  describe('Retry Logic', () => {\n    it('should retry failed requests', async () => {\n      const retryProcessor = new BatchProcessor({ retryAttempts: 3, retryDelay: 10 })\n      \n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      retryProcessor.addMultiple(items)\n      \n      mockExecutor\n        .mockRejectedValueOnce(new Error('First failure'))\n        .mockRejectedValueOnce(new Error('Second failure'))\n        .mockResolvedValueOnce([{ data: { success: true } }])\n      \n      const results = await retryProcessor.process(mockExecutor)\n      \n      expect(results.get('test-1')?.success).toBe(true)\n      expect(mockExecutor).toHaveBeenCalledTimes(3)\n    })\n\n    it('should fail after max retry attempts', async () => {\n      const retryProcessor = new BatchProcessor({ retryAttempts: 2, retryDelay: 10 })\n      \n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      retryProcessor.addMultiple(items)\n      \n      mockExecutor.mockRejectedValue(new Error('Persistent failure'))\n      \n      const results = await retryProcessor.process(mockExecutor)\n      \n      expect(results.get('test-1')?.success).toBe(false)\n      expect(results.get('test-1')?.error?.message).toBe('Persistent failure')\n      expect(mockExecutor).toHaveBeenCalledTimes(2)\n    })\n  })\n\n  describe('Batching and Concurrency', () => {\n    it('should split large batches into smaller chunks', async () => {\n      const smallBatchProcessor = new BatchProcessor({ maxBatchSize: 2 })\n      \n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/1' },\n        { id: 'test-2', method: 'GET', relativeUrl: 'campaign/2' },\n        { id: 'test-3', method: 'GET', relativeUrl: 'campaign/3' },\n        { id: 'test-4', method: 'GET', relativeUrl: 'campaign/4' },\n        { id: 'test-5', method: 'GET', relativeUrl: 'campaign/5' }\n      ]\n      \n      smallBatchProcessor.addMultiple(items)\n      \n      mockExecutor.mockResolvedValue([{ data: 'success' }])\n      \n      await smallBatchProcessor.process(mockExecutor)\n      \n      // Should be called 3 times: 2+2+1 items\n      expect(mockExecutor).toHaveBeenCalledTimes(3)\n      expect(mockExecutor.mock.calls[0][0]).toHaveLength(2)\n      expect(mockExecutor.mock.calls[1][0]).toHaveLength(2)\n      expect(mockExecutor.mock.calls[2][0]).toHaveLength(1)\n    })\n\n    it('should respect concurrency limits', async () => {\n      const concurrentProcessor = new BatchProcessor({ \n        maxBatchSize: 1, \n        maxConcurrent: 2 \n      })\n      \n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/1' },\n        { id: 'test-2', method: 'GET', relativeUrl: 'campaign/2' },\n        { id: 'test-3', method: 'GET', relativeUrl: 'campaign/3' },\n        { id: 'test-4', method: 'GET', relativeUrl: 'campaign/4' }\n      ]\n      \n      concurrentProcessor.addMultiple(items)\n      \n      let concurrentCalls = 0\n      let maxConcurrent = 0\n      \n      mockExecutor.mockImplementation(async () => {\n        concurrentCalls++\n        maxConcurrent = Math.max(maxConcurrent, concurrentCalls)\n        \n        await new Promise(resolve => setTimeout(resolve, 50))\n        \n        concurrentCalls--\n        return [{ data: 'success' }]\n      })\n      \n      await concurrentProcessor.process(mockExecutor)\n      \n      expect(maxConcurrent).toBeLessThanOrEqual(2)\n    })\n  })\n\n  describe('Timeout Handling', () => {\n    it('should timeout long-running operations', async () => {\n      const timeoutProcessor = new BatchProcessor({ timeout: 100 })\n      \n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      timeoutProcessor.addMultiple(items)\n      \n      mockExecutor.mockImplementation(() => \n        new Promise(resolve => setTimeout(resolve, 200))\n      )\n      \n      const results = await timeoutProcessor.process(mockExecutor)\n      \n      expect(results.get('test-1')?.success).toBe(false)\n      expect(results.get('test-1')?.error?.message).toBe('Operation timed out')\n    })\n  })\n\n  describe('Error Response Handling', () => {\n    it('should handle API error responses', async () => {\n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      processor.addMultiple(items)\n      \n      mockExecutor.mockResolvedValue([\n        {\n          error: {\n            message: 'Invalid OAuth access token',\n            code: 190,\n            type: 'OAuthException'\n          }\n        }\n      ])\n      \n      const results = await processor.process(mockExecutor)\n      \n      expect(results.get('test-1')?.success).toBe(false)\n      expect(results.get('test-1')?.error?.message).toBe('Invalid OAuth access token')\n      expect(results.get('test-1')?.error?.code).toBe(190)\n      expect(results.get('test-1')?.error?.type).toBe('OAuthException')\n    })\n\n    it('should handle transform errors', async () => {\n      const items: BatchItem[] = [\n        { \n          id: 'test-1', \n          method: 'GET', \n          relativeUrl: 'campaign/123',\n          transform: () => { throw new Error('Transform failed') }\n        }\n      ]\n      \n      processor.addMultiple(items)\n      \n      mockExecutor.mockResolvedValue([{ data: { success: true } }])\n      \n      const results = await processor.process(mockExecutor)\n      \n      expect(results.get('test-1')?.success).toBe(false)\n      expect(results.get('test-1')?.error?.message).toBe('Transform failed')\n      expect(results.get('test-1')?.error?.type).toBe('TRANSFORM_ERROR')\n    })\n  })\n\n  describe('Concurrent Processing Prevention', () => {\n    it('should prevent concurrent processing', async () => {\n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      processor.addMultiple(items)\n      \n      mockExecutor.mockImplementation(() => \n        new Promise(resolve => setTimeout(() => resolve([{ data: 'success' }]), 100))\n      )\n      \n      const promise1 = processor.process(mockExecutor)\n      \n      await expect(processor.process(mockExecutor))\n        .rejects.toThrow('Batch processing already in progress')\n      \n      await promise1\n    })\n  })\n\n  describe('State Management', () => {\n    it('should clear queue after processing', async () => {\n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      processor.addMultiple(items)\n      expect(processor.getQueueSize()).toBe(1)\n      \n      mockExecutor.mockResolvedValue([{ data: 'success' }])\n      \n      await processor.process(mockExecutor)\n      \n      expect(processor.getQueueSize()).toBe(0)\n    })\n\n    it('should return copy of results', () => {\n      const results1 = processor.getResults()\n      const results2 = processor.getResults()\n      \n      expect(results1).not.toBe(results2)\n      expect(results1).toEqual(results2)\n    })\n  })\n})\n\ndescribe('BatchBuilder', () => {\n  let builder: BatchBuilder\n\n  beforeEach(() => {\n    builder = new BatchBuilder()\n  })\n\n  describe('Campaign Operations', () => {\n    it('should add campaign insights request', () => {\n      builder.addCampaignInsights('123456789', {\n        fields: ['spend', 'impressions'],\n        datePreset: 'last_30d'\n      })\n      \n      const items = builder.build()\n      \n      expect(items).toHaveLength(1)\n      expect(items[0].id).toBe('campaign_insights_123456789')\n      expect(items[0].method).toBe('GET')\n      expect(items[0].relativeUrl).toBe('123456789/insights')\n      expect(items[0].params?.fields).toBe('spend,impressions')\n      expect(items[0].params?.date_preset).toBe('last_30d')\n    })\n\n    it('should add ad set insights request', () => {\n      builder.addAdSetInsights('987654321', {\n        fields: 'spend,clicks',\n        date_preset: 'last_7d'\n      })\n      \n      const items = builder.build()\n      \n      expect(items).toHaveLength(1)\n      expect(items[0].id).toBe('adset_insights_987654321')\n      expect(items[0].relativeUrl).toBe('987654321/insights')\n    })\n\n    it('should add multiple campaign requests', () => {\n      builder.addCampaigns(['123', '456', '789'], ['id', 'name', 'status'])\n      \n      const items = builder.build()\n      \n      expect(items).toHaveLength(3)\n      expect(items[0].id).toBe('campaign_123')\n      expect(items[1].id).toBe('campaign_456')\n      expect(items[2].id).toBe('campaign_789')\n      \n      items.forEach(item => {\n        expect(item.method).toBe('GET')\n        expect(item.params?.fields).toBe('id,name,status')\n      })\n    })\n  })\n\n  describe('Builder Operations', () => {\n    it('should chain operations', () => {\n      const items = builder\n        .addCampaignInsights('123')\n        .addAdSetInsights('456')\n        .addCampaigns(['789'])\n        .build()\n      \n      expect(items).toHaveLength(3)\n    })\n\n    it('should clear builder', () => {\n      builder.addCampaignInsights('123')\n      expect(builder.build()).toHaveLength(1)\n      \n      builder.clear()\n      expect(builder.build()).toHaveLength(0)\n    })\n  })\n\n  describe('Parameter Handling', () => {\n    it('should handle time range parameters', () => {\n      builder.addCampaignInsights('123', {\n        timeRange: { since: '2024-01-01', until: '2024-01-31' }\n      })\n      \n      const items = builder.build()\n      \n      expect(items[0].params?.time_range)\n        .toBe('{\"since\":\"2024-01-01\",\"until\":\"2024-01-31\"}')\n    })\n\n    it('should use default fields when none specified', () => {\n      builder.addCampaignInsights('123')\n      \n      const items = builder.build()\n      \n      expect(items[0].params?.fields).toContain('spend')\n      expect(items[0].params?.fields).toContain('impressions')\n      expect(items[0].params?.fields).toContain('clicks')\n    })\n  })\n})\n\ndescribe('MetaBatchExecutor', () => {\n  let executor: MetaBatchExecutor\n  const mockToken = 'test-access-token'\n\n  beforeEach(() => {\n    jest.clearAllMocks()\n    executor = new MetaBatchExecutor(mockToken)\n  })\n\n  describe('Batch Execution', () => {\n    it('should execute batch request to Meta API', async () => {\n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' },\n        { id: 'test-2', method: 'GET', relativeUrl: 'campaign/456' }\n      ]\n      \n      const mockResponse = {\n        ok: true,\n        json: async () => [\n          { body: '{\"data\":{\"id\":\"123\"}}', code: 200 },\n          { body: '{\"data\":{\"id\":\"456\"}}', code: 200 }\n        ]\n      }\n      \n      ;(global.fetch as jest.Mock).mockResolvedValue(mockResponse)\n      \n      const results = await executor.execute(items)\n      \n      expect(global.fetch).toHaveBeenCalledWith(\n        'https://graph.facebook.com/v19.0',\n        {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({\n            access_token: mockToken,\n            batch: JSON.stringify([\n              { method: 'GET', relative_url: 'campaign/123' },\n              { method: 'GET', relative_url: 'campaign/456' }\n            ])\n          })\n        }\n      )\n      \n      expect(results).toHaveLength(2)\n      expect(results[0]).toEqual({ data: { id: '123' } })\n      expect(results[1]).toEqual({ data: { id: '456' } })\n    })\n\n    it('should handle HTTP errors', async () => {\n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      ;(global.fetch as jest.Mock).mockResolvedValue({\n        ok: false,\n        statusText: 'Unauthorized'\n      })\n      \n      await expect(executor.execute(items))\n        .rejects.toThrow('Batch request failed: Unauthorized')\n    })\n\n    it('should handle non-JSON response bodies', async () => {\n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      const mockResponse = {\n        ok: true,\n        json: async () => [\n          { body: 'plain text response', code: 200 }\n        ]\n      }\n      \n      ;(global.fetch as jest.Mock).mockResolvedValue(mockResponse)\n      \n      const results = await executor.execute(items)\n      \n      expect(results[0]).toBe('plain text response')\n    })\n\n    it('should handle missing body in response', async () => {\n      const items: BatchItem[] = [\n        { id: 'test-1', method: 'GET', relativeUrl: 'campaign/123' }\n      ]\n      \n      const mockResponse = {\n        ok: true,\n        json: async () => [\n          { code: 200, headers: [] }\n        ]\n      }\n      \n      ;(global.fetch as jest.Mock).mockResolvedValue(mockResponse)\n      \n      const results = await executor.execute(items)\n      \n      expect(results[0]).toEqual({ code: 200, headers: [] })\n    })\n  })\n\n  describe('Request Formatting', () => {\n    it('should include item parameters in batch request', async () => {\n      const items: BatchItem[] = [\n        { \n          id: 'test-1', \n          method: 'GET', \n          relativeUrl: 'campaign/123',\n          params: { fields: 'id,name', limit: 10 }\n        }\n      ]\n      \n      ;(global.fetch as jest.Mock).mockResolvedValue({\n        ok: true,\n        json: async () => [{ body: '{}' }]\n      })\n      \n      await executor.execute(items)\n      \n      const fetchCall = (global.fetch as jest.Mock).mock.calls[0]\n      const requestBody = JSON.parse(fetchCall[1].body)\n      const batchItems = JSON.parse(requestBody.batch)\n      \n      expect(batchItems[0]).toMatchObject({\n        method: 'GET',\n        relative_url: 'campaign/123',\n        fields: 'id,name',\n        limit: 10\n      })\n    })\n\n    it('should include request body for POST/PUT requests', async () => {\n      const items: BatchItem[] = [\n        { \n          id: 'test-1', \n          method: 'POST', \n          relativeUrl: 'campaign/123',\n          body: { name: 'Updated Campaign' }\n        }\n      ]\n      \n      ;(global.fetch as jest.Mock).mockResolvedValue({\n        ok: true,\n        json: async () => [{ body: '{}' }]\n      })\n      \n      await executor.execute(items)\n      \n      const fetchCall = (global.fetch as jest.Mock).mock.calls[0]\n      const requestBody = JSON.parse(fetchCall[1].body)\n      const batchItems = JSON.parse(requestBody.batch)\n      \n      expect(batchItems[0].body).toEqual({ name: 'Updated Campaign' })\n    })\n  })\n\n  describe('Custom API Version', () => {\n    it('should use custom API version', async () => {\n      const customExecutor = new MetaBatchExecutor(mockToken, 'v18.0')\n      \n      ;(global.fetch as jest.Mock).mockResolvedValue({\n        ok: true,\n        json: async () => []\n      })\n      \n      await customExecutor.execute([])\n      \n      expect(global.fetch).toHaveBeenCalledWith(\n        'https://graph.facebook.com/v18.0',\n        expect.any(Object)\n      )\n    })\n  })\n})"